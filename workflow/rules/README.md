# Rules

Snakemake rule files live here.

Current Phase 1 rule set:

- `manifest.smk`: normalized manifest validation and metadata generation

Later phases will add stage-specific rule files such as `busco.smk`,
`alignment.smk`, and `species_tree.smk`.
